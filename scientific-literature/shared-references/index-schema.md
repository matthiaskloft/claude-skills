# `_index.json` Schema

Each top-level key is a citekey.

`references.md` and `references.bib` are derived outputs generated from this file.

```json
{
  "citekey": {
    "title": "string",
    "authors": ["string"],
    "authors_str": "string",
    "year": 2026,
    "journal": "string|null",
    "booktitle": "string|null",
    "publisher": "string|null",
    "pages": "string|null",
    "volume": "string|null",
    "issue": "string|null",
    "type": "string|null",
    "doi": "string",
    "url": "string|null",
    "arxiv_id": "string|null",
    "note": "string|null",
    "openalex_id": "string",
    "categories": ["string"],
    "source": ["string"],
    "pdf": "string|null",
    "apa7": "string",
    "added_at": "YYYY-MM-DD"
  }
}
```
